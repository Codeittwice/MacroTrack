/**
 * App version shown in Settings > About.
 * Keep in sync with package.json "version" by hand for now — flagged for Commander to replace
 * this with a Vite `define` (e.g. __APP_VERSION__) sourced from package.json at build time.
 */
export const APP_VERSION = '0.1.0';
